To execute this code, compile it in Linux,
gcc main.c -o main
and execute it with the command below.
./main -v number_of_vertices textfile

For example ./main -v 100 in7.txt
-v passes the number of vertices to the program
